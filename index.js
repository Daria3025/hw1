const name = "Daria";
const admin = name;
console.log(admin);

const days = Math.ceil(Math.random() * 10);
const seconds = `${days * 60 * 60 * 24} seconds`;
console.log(seconds);

const userAge = prompt("Please enter your age");
console.log(userAge);
